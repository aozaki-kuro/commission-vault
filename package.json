{
  "name": "nsfw-commission-neo",
  "type": "module",
  "version": "1.3.0",
  "private": true,
  "packageManager": "pnpm@11.5.2",
  "author": "Crystallize",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web:fact-source:export": "pnpm -C apps/admin-worker web:fact-source:export",
    "web:fact-source:sync-images": "pnpm -C apps/web fact-source:sync-images",
    "build": "pnpm run build:web",
    "build:all": "TURBO_TELEMETRY_DISABLED=1 turbo run build",
    "build:admin": "TURBO_TELEMETRY_DISABLED=1 turbo run build --filter=@commission-index/admin",
    "build:web": "TURBO_TELEMETRY_DISABLED=1 turbo run build --filter=@commission-index/web",
    "check": "TURBO_TELEMETRY_DISABLED=1 turbo run check --filter=@commission-index/web",
    "clean": "rm -rf node_modules .wrangler apps/web/.astro apps/web/dist coverage playwright-report test-results && pnpm i",
    "deploy": "pnpm run deploy:web",
    "deploy:admin": "pnpm -C apps/admin-worker deploy",
    "deploy:web": "pnpm -C apps/web deploy",
    "dev": "pnpm -C apps/web dev",
    "dev:admin": "tsx scripts/devAdminRemote.ts",
    "dev:admin:remote": "tsx scripts/devAdminRemote.ts",
    "dev:web": "pnpm -C apps/web dev",
    "dev:worker": "pnpm -C apps/admin-worker dev",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "prepare": "prek install",
    "preview": "pnpm -C apps/web preview",
    "test": "vitest run -c vitest.config.ts",
    "test:changed": "vitest run -c vitest.config.ts --changed",
    "test:visual": "playwright test -c playwright.config.ts",
    "test:visual:update": "playwright test -c playwright.config.ts --update-snapshots",
    "test:watch": "vitest -c vitest.config.ts",
    "typecheck": "TURBO_TELEMETRY_DISABLED=1 turbo run typecheck"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^9.0.0",
    "@eslint-react/eslint-plugin": "^5.9.0",
    "@j178/prek": "^0.4.4",
    "@playwright/test": "^1.60.0",
    "@tailwindcss/vite": "^4.3.1",
    "@types/node": "^25.9.3",
    "eslint": "^10.5.0",
    "eslint-plugin-astro": "^1.7.0",
    "eslint-plugin-format": "^2.0.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4",
    "tailwindcss": "^4.3.1",
    "tsx": "^4.22.4",
    "turbo": "^2.9.18",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.100.0"
  },
  "lint-staged": {
    "*": [
      "eslint --fix"
    ],
    "*.{md,mdx}": [
      "prettier --write"
    ]
  }
}
